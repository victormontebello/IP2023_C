#include <stdio.h>

int main(){

    int a,b,i;
    scanf("%d %d", &a, &b);
    if(a%2!=0)
        printf("O PRIMEIRO NUMERO NAO E PAR");
    else
        while(b--)
            printf("%d", i);
            i+=2;

    return 0;
}